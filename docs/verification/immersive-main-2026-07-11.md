# Immersive main verification

This branch exists only to run the repository's pull-request quality workflow against the current `main` source tree.

It does not change runtime code and should not be merged.
